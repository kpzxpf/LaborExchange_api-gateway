spring:
  jwt:
    secret: test-secret-key-for-testing-only-32-chars-min

  cloud:
    gateway:
      routes: []

  data:
    redis:
      host: localhost
      port: 6379

secret-jwt-key: test-secret-key-for-testing-only-32-chars-min

app:
  cors:
    allowed-origins: "http://localhost:3000"

rate-limit:
  general:
    max-requests: 100
    window-ms: 60000
  auth:
    max-requests: 20
    window-ms: 60000
